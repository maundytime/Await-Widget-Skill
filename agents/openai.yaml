interface:
  display_name: "Await Widget"
  short_description: "Build AwaitJS iOS widgets in TSX."
  default_prompt: "Use $await-widget-skill to create or modify an AwaitJS widget."

policy:
  allow_implicit_invocation: true
